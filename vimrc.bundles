" Contains the vim bundles as seen on
" https://github.com/thoughtbot/dotfiles/blob/master/vimrc.bundles

if &compatible
  set nocompatible
end

" Automatically installs the plugins when it finds a new one
if empty(glob('~/.vim/autoload/plug.vim'))
  silent !curl -fLo ~/.vim/autoload/plug.vim --create-dirs
    \ https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
  autocmd VimEnter * PlugInstall | source $MYVIMRC
endif

call plug#begin('~/.vim/bundle')

" == General editor plugins ==
Plug 'bling/vim-airline'               " Status bar
Plug 'christoomey/vim-tmux-navigator'  " integrates tmux and vim navigation
Plug 'Raimondi/delimitMate'            " Auto close quotes, parens, brackets
Plug 'scrooloose/nerdtree'             " File tree
Plug 'tpope/vim-commentary'            " Comment/uncomment lines
Plug 'tpope/vim-fugitive'              " Git integration
Plug 'tpope/vim-surround'              " Mappings to change surroundings
Plug 'tpope/vim-unimpaired'            " Mappings to brackets
Plug 'vimwiki/vimwiki'

" == Syntax highlighting ==
Plug 'mxw/vim-jsx'                     " JSX syntax highlighting
Plug 'kchmck/vim-coffee-script'        " Coffescript syntax
Plug 'pangloss/vim-javascript'         " JS syntax and indent plugins
Plug 'rust-lang/rust.vim'              " Rust syntax

" == Color Schemes ==
Plug 'morhetz/gruvbox'
Plug 'arcticicestudio/nord-vim'

if filereadable(expand("~/.vimrc.bundles.local"))
  source ~/.vimrc.bundles.local
endif
  
call plug#end()

